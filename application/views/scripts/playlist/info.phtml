<?

if (isset($this->playlistRow)) {
    echo Zend_Json::encode(
        array(
            $this->playlistRow->id,
            $this->playlistInfo($this->playlistRow)
        )
    );
} elseif (isset($this->message)) {
    echo Zend_Json::encode($this->message);
}

?>
